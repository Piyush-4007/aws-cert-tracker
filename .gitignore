node_modules
.next
out
.DS_Store
*.tsbuildinfo
next-env.d.ts

# vercel
.vercel
.env*

# Guard: third-party character art dropped in for reference must never be
# committed to a public repo, deliberately or by an `add -A`.
public/icons/squirtle.png
